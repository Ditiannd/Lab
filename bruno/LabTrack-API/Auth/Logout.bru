meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/logout
  body: none
}

headers {
  Accept: application/json
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("token", "");
  }
}
